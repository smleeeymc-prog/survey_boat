# 인수인계 — 그래도, 여기 살고 있습니다

> 참여형 아카이브 프로토타입. Three.js 씬 + 설문 UI가 **`index.html` 단일 파일**에 들어 있다.
> 새 세션은 여기부터 읽으면 된다.

---

## 0. 먼저 알아야 할 것

### 작업 방식 (사용자 지시 사항)

1. **PR을 만들지 않는다. 브랜치도 파지 않는다.** `main`에서 바로 작업하고 커밋 후 푸시한다.
   되돌리기 어렵거나 범위가 큰 변경만 푸시 전에 한 번 확인받는다.
2. **결과만 보고한다.** 작업 과정을 하나씩 나열하지 말고, 무엇이 바뀌었는지와
   사용자가 판단해야 할 항목만 정리해서 준다. 세부 계산을 본문에 늘어놓지 않는다.
   (단, 사용자가 요청한 산출물 — 보고서·문서·워크스루 — 은 전문을 준다.)
3. **커밋 메시지는 한국어로**, 무엇을 왜 바꿨는지 적는다. 기존 커밋 로그를 참고할 것.
4. **코드 주석도 한국어.** "무엇을"이 아니라 "왜"를 쓴다.

이 항목들은 원래 작업 PC의 로컬 메모리에만 있던 것이라, 클라우드 세션으로는 따라가지 않는다.
그래서 여기에 옮겨 적었다.

### 배포

- GitHub: `smleeeymc-prog/survey_boat` (main)
- **Vercel 자동 배포**: https://survey-test-tau.vercel.app
- `main`에 푸시하면 1~2분 내 반영. **푸시 = 실서비스 반영이다.**

### 보류 항목 — 먼저 손대지 말 것

**캐빈 창문 조명.** 배 캐빈에 창문을 만들고 밤에 불이 켜지게 하는 작업.
사용자가 **먼저 모델링을 마친 뒤에** 진행하기로 했다.
새 GLB를 받으면 그때 창문 메쉬를 찾아 emissive + PointLight를 붙인다.
시간대 팔레트의 `night` 항목과 연동될 부분이다. 조명 베이크도 검토 대상이었다.

---

## 1. 이 프로젝트가 무엇인가

**입다 · 2026 다원예술 프로젝트** — "그래도, 여기 살고 있습니다"

충남·아산에 남아 살아가는 청년의 이야기를 **한 문장**으로 모으는 참여형 아카이브다.
관객이 직접 문장을 남기고, 그 문장이 다른 사람들의 기록과 함께 쌓이는 구조다.

- 여는 질문: **"지금, 당신은 이곳에 머물고 있나요, 지나가고 있나요?"**
- 수집물의 쓰임: **전시장 내 화면 · 웹 아카이브 · 아카이브북 · 결과보고서** (동의 화면에 명시)
- 아카이브 화면 이름: **"머무름의 지도"**

즉 이건 설문이 아니라 **전시 작품의 참여 인터페이스**다.
그래서 데이터를 많이 받는 것보다, 답하는 동안의 경험이 성립하는 게 우선이다.
문항이 적고(6단계), 문장이 80자로 제한되고, 익명이 기본값인 것도 그 때문이다.

### 개인정보를 최소로 받는다

- 지역은 넓은 범주만 — "상세 주소는 수집하지 않아요"
- 이름은 **선택**이며 익명이 기본. 별칭도 허용
- 공개 범위와 활용처를 동의 화면에서 명시하고, 동의 없이는 제출 버튼이 잠긴다

이 원칙을 무너뜨리는 방향(필수 입력 추가, 식별정보 수집 등)은 임의로 넣지 말 것.

---

## 2. 단계별 목적

| 단계 | 묻는 것 | 왜 이 질문인가 | 씬에서 일어나는 일 |
|---|---|---|---|
| `splash` | — | 프로젝트 소개 | 배가 정박한 기본 화면 |
| `onboard` | — | "머물고 있나요, 지나가고 있나요" 한 줄로 태도를 환기 | 기본 시점 |
| `region` | 활동 지역 | 아산 중심이되 인접·외부도 받아 **범위를 지도로 만들기 위함** | 카메라가 오두막·지형 쪽으로 이동. 섬이 화면 우측을 채운다 |
| `state` | 지금의 상태 | 머무름을 이분법(남는다/떠난다)이 아니라 **5가지 결**로 본다 | 배의 방향·속도·파도가 바뀐다. 섬이 있거나 멀어진다 |
| `sentence` | 머무르게 하는 것 (80자) | **이 프로젝트의 본체.** 나머지는 이 한 문장을 감싸는 맥락이다 | 기본 시점 |
| `share` | 이 마음을 몇 명과 나눠봤나 | 말해본 적 있는지를 통해 **그 마음이 얼마나 혼자만의 것인지**를 본다 | **하루의 시간대가 바뀐다** (아래 3장) |
| `keywords` | 문장을 설명하는 키워드 (최대 2개) | 문장을 분류 가능한 축으로 환원 — 아카이브 필터용 | 카메라가 배로 바짝. 캐빈 색과 소품이 바뀐다 |
| `consent` | 공개 방식 + 활용 동의 | 전시·출판에 쓰이므로 필수 절차 | 기본 시점 |
| `result` | — | 내 문장을 하나의 소장품처럼 보여준다 | **유리병이 씬을 감싼다 (ship in a bottle)** |
| `archive` | — | 다른 사람들의 기록과 함께 쌓인 것을 확인 | 씬 종료, 카드 벽 |

---

## 3. 씬이 무엇을 나타내는가

> 아래 해석은 **코드와 화면 문구에서 읽어낸 것**이다.
> 기획 의도와 어긋나는 부분이 있으면 사용자에게 확인하고 이 문서를 고칠 것.

| 요소 | 나타내는 것 |
|---|---|
| **배** | 답하는 사람 자신. 상태에 따라 정박하거나 떠나거나 오간다 |
| **섬 · 오두막 · 바위** | "이곳" — 머무는 자리, 돌아올 곳 |
| **배와 섬의 거리** | 이곳과의 관계. 떠나면 섬이 멀어지고, 정하지 못하면 아예 사라진다 |
| **하루의 시간대** | 그 마음을 얼마나 나눴는지. **많이 나눌수록 낮, 아무와도 안 나눴으면 밤** |
| **캐빈 색 / 갈매기 · 튜브** | 고른 키워드 |
| **유리병** | 제출된 기록. 풍경이 병 안에 봉해져 하나의 소장품이 된다 |

### 상태(state)별 배의 동작

| 상태 | 배 | 섬 | 파도 | 방향 |
|---|---|---|---|---|
| 머무르는 중 | 제자리 정박, 미세하게 흔들림 | 있음 | 잔잔 (0.22) | 섬을 등짐 |
| 떠날 준비 중 | 제자리, 연기 뿜음 | 있음 | 중간 (0.55) | 섬을 등짐 |
| 오가는 중 | 계속 항해 (등속) | **멀어짐** | 잔잔 (0.1) | 섬을 등짐 |
| 돌아온 사람 | 먼 바다에서 해안으로 6.5초간 접근 | 있음 | 거침 (0.75) | **섬을 향함** |
| 아직 모르겠음 | 제자리 표류 | **멀어짐** | 잔잔 (0.1) | **섬을 향함** |

'아직 모르겠음'이 '돌아온 사람'과 같은 방향을 보는 것은 의도된 것이다 —
돌아올 마음은 있으나 아직 정하지 못한 상태를 나타낸다.

### 시간대(share) 매핑

| 답 | 시간 | 뜻 |
|---|---|---|
| 여러 사람에게 | 낮 | 이미 널리 나눈 마음 |
| 가까운 사람에게 | 오후 | 가까운 이들에게만 |
| 한 두 명만 | 저녁 | 겨우 몇 사람 |
| 나눈 적 없음 | 밤 | 아직 혼자만 아는 마음 |

하늘·안개·바다색·조명·물에 비치는 반사색이 전부 함께 바뀐다.
저녁에만 그림자가 지는 것도 이 연출의 일부다.

### 확인이 필요한 것 (사용자만 아는 부분)

이 문서를 쓴 시점에 **코드에서 읽히지 않는 기획 정보**가 몇 가지 있다.
새 세션이 큰 결정을 내려야 할 때는 아래를 먼저 물어볼 것.

- 전시 형태 (관객이 현장 키오스크로 쓰는가, 개인 휴대폰으로 쓰는가)
- 목표 응답 수와 운영 기간
- 실제 백엔드 계획 — 지금 `entries`는 **목업 데이터이고 저장이 안 된다**
  (화면 우상단 "PROTOTYPE · 목업 데이터" 배지가 이 뜻이다)
- 아카이브북·결과보고서에서 이 데이터를 어떤 형태로 쓸 것인지
- 5가지 상태 구분과 10개 키워드 목록의 근거 (선행 리서치가 있는지)

---

## 4. 구조

```
survey_boat/
├─ index.html          전부 여기 있다 (2,345줄)
├─ HANDOFF.md          이 문서
└─ assets/Scene.glb    블렌더 모델 (Ship / Rock / Beachhouse / Seagull / Tube)
```

`index.html` 안은 크게 셋으로 나뉜다.

| 영역 | 내용 |
|---|---|
| `<style>` | 설문 UI 스타일. 스냅 피커, 하단 바, 아카이브 카드 |
| `<script>` (모듈 아님) | 설문 흐름. `render()`가 단계마다 화면을 통째로 다시 그린다 |
| `<script type="module">` | Three.js 씬 전부. `BottleScene` 클래스 하나 |

### 설문 흐름

```
splash → onboard → region → state → sentence → share → keywords → consent → result → archive
```

- `render()`가 `app.innerHTML=""` 후 전체를 다시 만든다.
  그래서 **입력 중에는 `render()`를 부르면 안 된다** — 포커스와 스크롤 위치가 날아간다.
- 피커는 `onPick`에서 `render()`를 부르지 않고 상태만 바꾼다.
  동의 화면의 이름 입력칸만 `renderNameField()`로 부분 갱신한다.
- 질문 단계마다 카메라 시점이 바뀐다(`CAM_STEPS`).
- `share` 단계(이 마음을 몇 명과 나눠봤나요?)의 답이 **하루의 시간대**를 결정한다.
  여러 사람에게=낮 / 가까운 사람에게=오후 / 한 두 명만=저녁 / 나눈 적 없음=밤.

---

## 5. 지금 값들 (전부 상수로 빠져 있다)

### 배 / 배치

| 상수 | 값 | 뜻 |
|---|---|---|
| `SHIP_SCALE` | 3.38 | 배 크기. **연기 크기도 여기에 곱해진다** |
| `SHIP_DRAFT` | 0.17 | 흘수. `SHIP_SCALE`과 함께 곱해져 잠기는 비율이 유지된다 |
| `ISLAND_CLEARANCE` | 0.12 | 뱃머리와 오두막 사이 여유. 줄이면 배가 덱에 붙는다 |

### 카메라

각도는 **어느 단계에서도 바뀌지 않는다**: `baseYaw = -0.45`, `basePitch = 0.60`.
바뀌는 건 바라보는 지점(target)과 거리(radius)뿐이라 전환이 회전이 아니라 이동으로 보인다.

| 프리셋 | 값 |
|---|---|
| 기본 | `{ target: [0.0, 0.22, 0.0], radius: 9.7 }` |
| 지역 | `{ target: [1.30, 1.35, -0.30], radius: 12.2 }` |
| 키워드 | `{ target: [0.0, 0.45, 0.0], radius: 8.4 }` — 배가 화면 가로의 약 70%를 채우도록 맞춘 값 |
| 결과 | `{ target: [0.0, 0.58, 0.0], radius: 13.2 }` |

- 전환 `CAM_MOVE_SEC = 1.1`, easeInOutCubic
- `?debug=1`로 접속하면 현재 yaw/pitch/radius가 화면에 뜬다. 드래그로 각도를 잡고 그 값을 붙여넣으면 된다.
- 병이 떠오르면 각도 제한이 풀리고 병 중심을 축으로 자유롭게 아크를 돈다.

### 하늘

`scene.background`의 `CanvasTexture`는 **화면에 늘려 붙는 그림**이다. 3D 하늘 돔이 아니다.
그라디언트 정지점은 화면 세로 위치이고 월드 수평선과 무관하다. 자세한 건 11장.

### 바다 / 안개

- 타일: `const SEGMENTS = 71, SIZE = 31.5`
- 안개: `fogRangeFor = (radius) => ({ near: radius + 2.5, far: radius + 7.0 })`
  — 카메라 거리에 물려 있어 줌아웃해도 주인공이 안 묻힌다
- 반짝임: `uSpecPower = 400.0`, `uSpecStrength = 3.0`
  (지수가 낮을수록 넓게 퍼진다. 타일 크기를 바꾸면 다시 잡아야 한다)

### 연기

| 상수 | 값 |
|---|---|
| `SMOKE_COUNT` | 12 |
| `SMOKE_INTERVAL` | 0.26 |
| `SMOKE_LIFE` | 2.7 |
| `SMOKE_START` / `SMOKE_END` | 0.020 / 0.098 |
| `SMOKE_RISE` / `SMOKE_LEAN` | 0.24 / 0.07 |
| `SMOKE_SOLID_UNTIL` | 0.68 |

### 시간대 / 섬 이동

- `SHORE_EASE = cubicBezier(0.45, 0.02, 0.25, 1.0)` — **사용자가 블렌더로 조정하고 싶어 하는 값**
- `SHORE_MOVE_SEC = 2.2`, 섬은 `islandRestX + 12`까지 멀어진다
- 시간대 전환 `TIME_MOVE_SEC = 2.6`, easeInOutQuint
- 그림자 짙기(낮/오후/저녁/밤): `0.0 / 0.0 / 0.34 / 0.0` — **저녁에만 켜진다**
- 병: `BOTTLE_SCALE = 1.9`, `BOTTLE_Y = 0.55`

---

## 6. 반드시 알아야 할 함정

여기 적힌 건 **전부 실제로 한 번씩 겪고 고친 것**이다. 되돌리지 말 것.

### 6.1 파도 위상을 시간에 비례시키면 안 된다
위상을 `(speed + flowBoost) * t`로 두면 `flowBoost`가 바뀔 때 위상차가 `Δflow × t`가 되어
**페이지를 오래 켜둘수록 폭발한다.** 60초 시점에 모드를 바꾸면 120라디안이 0.7초에 몰려들었다.
흐름은 속도이므로 **위상으로 적분해 누적**한다(`_flowPhase`).
셰이더의 `uFlowPhase`와 JS의 `_waveHeightAt`이 **같은 식**이어야 한다 — 어긋나면 배가 수면에서 뜬다.

### 6.2 물은 커스텀 셰이더라 안개가 자동으로 안 걸린다
`scene.fog`는 "쓰겠다"고 선언한 재질에만 적용된다.
`fog: true` + Three.js 안개 청크를 직접 끼워넣어야 한다. 빼면 수평선이 직선으로 잘린다.

### 6.3 안개만으로는 경계가 안 사라진다
안개가 다 껴도 **"안개색 판"이 남아서** 하늘과 만나는 자리에 선이 보인다.
먼 바다는 알파를 0으로 빼서 하늘이 비치게 한다(`uFadeNear` / `uFadeFar`).
**섬도 같은 이유로** 멀어질 때 알파를 뺀다(`islandMats`).

### 6.4 그림자는 배만 드리우고 수면만 받는다
물체가 자기 그림자를 받으면 **자글자글한 줄무늬(섀도 아크네)** 가 생긴다.
섬은 그림자에서 완전히 뺐고, 배는 드리우기만 하고 받지 않는다.
물은 커스텀 셰이더라 그림자를 못 받으므로 **`ShadowMaterial` 평면**을 수면 위에 깔아 대신 받는다.
해와 그림자 절두체는 배에 붙어 있다 — 안 그러면 '오가는 중'에서 배가 멀어질 때 그림자가 사라진다.

### 6.5 후처리를 켜면 안티에일리어싱이 죽는다
`antialias: true`인데도 `EffectComposer`를 거치면 MSAA 없는 렌더 타깃에 그리게 되어
**가장자리가 자글자글해진다.** 이것 때문에 피사계 심도(블러)를 넣었다가 걷어냈다.
다시 넣을 거면 이 대가를 알고 넣어야 하고, `OutputPass`를 반드시 마지막에 붙여야 한다
(없으면 화면 전체가 어두워진다).

### 6.6 반투명이 겹치면 지저분해진다
반투명 덩어리가 여러 겹 겹치면 겹친 수만큼 알파가 쌓여 안쪽 실루엣이 드러난다.
연기는 수명의 68%까지 **완전 불투명 + depthWrite**로 렌더하고,
마지막 구간에서만 투명해지면서 색을 안개색 쪽으로 민다.

### 6.7 피커는 네이티브 스크롤이다 — 직접 구현하지 말 것
한 번 직접 구현했다가 되돌렸다. 문제는 네이티브가 아니라 **네이티브와 싸운 코드**였다.

- `scroll-snap-type`은 **축을 명시**한다 (`y mandatory` / `x mandatory`). `both`는 축이 섞인다.
- 놓는 순간 **우리가 스크롤을 옮기면 안 된다.** 브라우저 스냅과 겹쳐 두 칸씩 튄다.
- 여러 칸 건너뛰는 건 **`scroll-snap-stop: always`** 로 막는다.
- **터치는 가로채지 않는다** (`pointerType` 검사). 건드리면 네이티브 관성이 죽는다.
- 가로 피커의 양끝 여백은 **컨테이너 padding이 아니라 첫/마지막 항목의 margin**으로 준다.
  스크롤 컨테이너의 끝쪽 padding은 브라우저에 따라 `scrollWidth`에 안 잡혀서
  마지막 항목이 가운데까지 오지 못하고 치우친다.
- `#app`은 `pointer-events: none`이고 특정 셀렉터만 되살린다.
  **새 조작 요소를 넣으면 그 목록에 추가**해야 한다. 안 그러면 드래그가 캔버스로 새서 시점만 돈다.

### 6.8 GLB 노드는 씬 좌표를 들고 온다
`Ship` 노드가 GLB의 씬 좌표(x≈-0.60)를 그대로 갖고 있어서, 그냥 붙이면 배가 앵커에서
떨어진 자리에 그려지고 **헤딩 회전 시 반경 0.6짜리 원을 휘돈다.**
로컬 좌표를 0으로 비우고, 섬을 같은 만큼 밀어 블렌더 배치를 보존한다.
`Seagull` / `Tube`는 `attach()`로 옮긴다 — `add()`는 로컬 좌표로 재해석돼 엉뚱한 데로 간다.

### 6.9 병 클리핑은 프로파일을 따라야 한다
병은 축을 따라 반지름이 변하는 회전체다. 상수 반지름으로 자르면 좁은 목 쪽에서
**물이 직사각형으로 뚫고 나온다.**
`BOTTLE_PROFILE` 하나를 LatheGeometry와 물 셰이더가 공유한다.

### 6.10 배열 판정에 `instanceof`를 쓰지 말 것 (JSON 직렬화 시)
값을 만든 쪽의 생성자와 비교하므로 다른 실행 컨텍스트에서 온 배열에 `false`가 난다.
Three.js가 돌려주는 값이 여기 걸릴 수 있다.

---

## 7. 자기검증

`index.html` 맨 아래 `selfCheck()`가 있다. 콘솔에 빨간 assert가 뜨면 뭔가 어긋난 것이다.

1. 파도 랩어라운드가 위상 불변인가 (`WAVE_WRAP_DOMAIN = 1144`)
2. JS와 GLSL의 파도 정의가 일치하는가
3. 흐름 세기를 바꿔도 파도가 제자리에서 변형되는가 (6.1의 회귀 방지)
4. 물 재질에 안개 배선이 살아 있는가
5. 최대 줌아웃에서 안개가 타일 끝보다 먼저 짙어지는가

**새 기능을 넣고 콘솔이 조용한지 반드시 확인할 것.**

---

## 8. 남은 것

### 눈으로 확인하지 못한 값들

최근 여러 커밋은 **브라우저 도구가 끊긴 상태에서 작업했다.**
문법과 수치 관계만 정적으로 확인했고 **실제 렌더는 보지 못했다.**
아래는 감으로 잡은 값이라 조정이 필요할 수 있다.

- 시간대 4종의 톤
- **[진단 완료] 저녁 자주색 띠는 지금 보이지 않는다.** 하늘은 화면에 붙은 그림이라
  그라디언트 0.78 지점은 화면 아래쪽이고, 거기는 바다가 완전히 가린다.
  정지점을 하늘이 실제로 보이는 위쪽(0~0.4)으로 올려야 한다. 11장 참조
- 그림자 짙기(저녁 0.34)와 그림자 평면 높이(0.16)
- 연기 크기·속도·밀도
- 섬 페이드가 시작되는 지점 (여정의 50%)
- 병이 떠오른 뒤의 아크 회전 감각
- 카메라 프리셋 거리

### 사용자가 조정하고 싶어 하는 것

**`SHORE_EASE` 곡선.** 블렌더 그래프 에디터에서 F-커브 핸들을 잡아 보내주기로 했다.
프레임과 값을 각각 0~1로 정규화한 좌표 네 개면 된다.
변환 방법은 `cubicBezier` 함수 주석에 적어뒀다. 스크린샷으로 받아 수치로 옮겨도 된다.

### 알려진 미해결 버그

없다. 보고된 것은 모두 처리했다.

---

## 9. 비용 주의

작업 중 스크린샷을 많이 찍었더니 업로드가 수백 MB로 불어났다.
**툴 호출 하나가 API 요청 하나이고, 요청마다 대화 전체(이미지 포함)가 실려 간다.**

- **스크린샷을 찍지 않는다.** 사용자가 배포본을 보고 피드백을 준다.
- 꼭 봐야 하면 저해상도로 1~2장만.
- **bash 호출을 한 번에 묶는다.** 나눠 부를수록 요청 수가 늘어난다.

---

## 10. 무엇을 참고했나

### 시각 레퍼런스 (사용자가 제시)

시간대 4종의 톤은 **사용자가 보낸 레퍼런스 이미지 순서대로** 매칭한 것이다.
색을 임의로 고른 게 아니므로, 톤을 바꿀 때는 원본 레퍼런스를 다시 확인하고 맞출 것.

| 레퍼런스 | 반영된 곳 |
|---|---|
| 로우폴리 해변 섬 (야자수·파라솔·요트, 밝은 청록) | **낮** 팔레트 |
| 로우폴리 해변 + 금빛 태양 반사 길 | **오후** 팔레트 |
| 로우폴리 노을 해변 (주황·핑크 위 청록 하늘) | **저녁** 팔레트, 지평선 자주색 띠 |
| 야간 로우폴리 해변 (달, 랜턴 불빛 점점이) | **밤** 팔레트, 그리고 **보류 중인 캐빈 창문 조명**의 목표 이미지 |
| "3D LOW POLY SMOKE" (겹치는 로우폴리 구 덩어리) | **굴뚝 연기** — 구 하나가 아니라 겹친 덩어리로 간 이유 |
| 아이폰 타이머 설정 화면 | **세로 스냅 피커** — 하나만 보이고 굴려서 고르는 방식 |
| 좌우 스와이프 선택 UI (양옆 항목이 흐리게 걸침) | **가로 피커** — 선택 항목 중앙, 앞뒤는 블러 |

### 기술 레퍼런스

| 무엇 | 어디에 |
|---|---|
| **dli/waves — Tessendorf 오션 데모** | 물 셰이더의 프레넬 + 램버트 + 스펙큘러 조합이 여기서 왔다. 텍스처 없이 수식만으로 바다를 만드는 접근. **원래 코드에 출처 주석이 있었는데 리팩터링 중 지워졌다** |
| Gerstner 파도 | 물 표면 변위. 파장 2.2 / 1.3 / 0.8 세 개를 겹친다 |
| Three.js 안개 청크 (`fog_pars_*`, `fog_*`) | 커스텀 셰이더에 안개를 물릴 때 직접 끼워넣는다 (6.2) |
| `THREE.ShadowMaterial` | 물 셰이더를 건드리지 않고 수면에 그림자를 받는 방법 (6.4) |
| CSS `scroll-snap-stop: always` | 한 제스처에 한 칸만 넘어가게 하는 표준 해법 (6.7) |
| CSS `cubic-bezier` ↔ 블렌더 F-커브 | `SHORE_EASE`. 두 형식이 같은 구조라 블렌더에서 잡은 곡선을 그대로 옮길 수 있다 |
| Three.js `OutputPass` | 후처리 사용 시 색공간 복원용. 지금은 후처리를 걷어내서 미사용 (6.5) |

### 외부 의존

```
https://unpkg.com/three@0.160.1/build/three.module.js
https://unpkg.com/three@0.160.1/examples/jsm/
```

CDN 직접 로드. 빌드 도구도 패키지 매니저도 쓰지 않는다.
`index.html`을 브라우저로 열면 그대로 돈다(단, GLB 로드 때문에 `file://`이 아니라 http 서버가 필요).

### 우리가 직접 분석한 것

값을 감으로 정한 게 아니라 실제로 재서 정한 것들이다. 다시 조정할 때 같은 방법을 쓰면 된다.

**GLB 구조 분석** — `assets/Scene.glb`의 노드 트리와 월드 바운딩박스를 직접 파싱해서 얻었다.

| 노드 | 월드 위치 / 크기 |
|---|---|
| `Ship` | 중심 (-0.54, 0.30, 0.15), 크기 0.86 × 0.46 × 0.31 |
| `Rock` | 중심 (0.86, 0.40, -0.35), 크기 2.92 × 2.26 × 2.93 |
| `Beachhouse` | 중심 (0.36, 0.43, -0.28), 크기 1.67 × 2.13 × 1.41 |
| `Seagull` | 배 바로 위 (-0.73, 0.51, 0.14), 아주 작음 (0.047) — 4마리 중 가장 큰 하나만 남겼다 |
| `Tube` | 뱃전 (-0.64, 0.20, 0.29) |

이 수치들로 배 확대 시 충돌 여부, 섬 이격 거리, 병이 배를 담는지를 전부 계산했다.

**측정으로 잡은 값들**

- 파도 위상 폭주: 모드 전환 시 프레임당 배 높이 변화 **0.148 → 0.001** (파도 진폭 0.093보다 컸다)
- '오가는 중' 전환 요동: 프레임당 **0.148 → 0.00105**
- 배 화면 가로 점유율: 키워드 화면에서 **약 71%** (radius를 바꿔가며 투영 좌표로 실측)
- 그림자 길이: 저녁 해 고도 17도면 길이 3.91로 절두체(±3.5)를 넘어 잘렸다 → 고도 28도, 절두체 ±5로 조정
- 병 프로파일별 수면 허용 폭: 몸통 1.26 → 목 0.18 → 목 끝 0 (상수 반지름이면 전 구간 1.26으로 고정되어 직사각형으로 뚫고 나왔다)
- 안개 vs 타일 끝 거리: 전 프리셋에서 바다가 15~20에서 사라지고 타일 끝은 22~27
- 이징 곡선: 진행률 10% 시점에 예전 지수 감쇠는 **39%** 이동, 현재 베지어는 **2.4%**

### 폐기한 접근 (다시 시도하지 말 것)

| 시도 | 왜 버렸나 |
|---|---|
| **피사계 심도(BokehPass)** | `EffectComposer`를 거치면 MSAA가 죽어 화질이 눈에 띄게 떨어진다 (6.5) |
| **피커 직접 구현** (transform 기반) | 네이티브 관성이 죽는다. 문제는 네이티브가 아니라 네이티브와 싸운 코드였다 (6.7) |
| **컨베이어 방식 무한 바다** | 지오메트리를 옆으로 밀어내다 보니 타일 끝이 드러났다. 타일을 배 위치로 재중심하는 방식으로 대체 |
| **상수 반지름 병 클리핑** | 병이 회전체라 목 쪽에서 물이 뚫고 나왔다 (6.9) |
| **마스트 + 깃발** (절차적 생성) | 배 바운딩박스 꼭대기에 매달려 공중에 떠 보였다. 키워드 표시는 갈매기·튜브가 대신한다 |

---

## 11. 검증할 때 알아야 할 것

### 하늘은 3D가 아니다 — 화면에 붙은 그림이다

`scene.background`에 `CanvasTexture`를 넣으면 three.js는 그걸 **화면 전체에 늘려 붙인다.**
하늘 돔이 아니라 배경 이미지다. 그래서:

- `paintSky`의 그라디언트 정지점(0 / 0.48 / 0.78 / 1)은 **화면 세로 위치**다. 월드 좌표가 아니다.
- **3D 수평선과는 아무 상관이 없다.** 수평선 위치를 계산해서 띠 위치를 맞추려 하면 헛돈다.

**3D 수평선은 기본 시점에서 화면에 없다. 이건 설계된 결과다.**
`basePitch = 0.60`(34.4도 내려다봄)인데 세로 FOV가 34도라 시야 반각이 17도다.
수평선은 시선축보다 34.4도 위에 있으므로 화면 위쪽 바깥으로 나간다.
화면에 보이는 "하늘"은 **먼 바다가 알파로 지워져 비치는 부분**(6.3)뿐이고, 화면 위쪽 35~40% 정도다.

**여기서 나오는 미해결 문제** — 저녁 자주색 띠를 그라디언트 0.78 지점에 넣었는데,
그 자리는 화면 아래쪽이라 **바다에 완전히 가려서 보이지 않는다.**
고치려면 카메라가 아니라 **정지점을 위로 올려야 한다** (하늘이 실제로 보이는 0~0.4 구간 안으로).

### 클라우드 세션은 작업 전에 푸시 권한부터 확인할 것

클라우드 컨테이너의 GitHub 토큰은 **세션이 시작될 때 한 번 발급**된다.
세션이 도는 도중에 커넥터 권한을 바꿔도 그 세션에는 내려오지 않는다.
읽기만 되고 쓰기가 403이면 커밋을 아무리 쌓아도 밀 수 없고, 컨테이너와 함께 사라진다.

**그러니 코드를 건드리기 전에 한 번 확인한다.**

```
git push --dry-run origin main
```

403이 나면 그 세션에서는 푸시가 불가능하다. 작업을 시작하지 말고 사용자에게 알린다.
고치는 방법은 두 가지뿐이다.

1. claude.ai 커넥터 설정에서 GitHub 연결이 이 저장소에 **Contents: Read and write** 권한을
   갖고 있는지 확인한다. 읽기 전용이면 새 세션을 열어도 똑같이 실패한다.
2. 권한이 맞다면 **새 세션을 연다.** 시작 시점에 갱신된 권한으로 토큰이 발급된다.

참고로 이건 저장소 잠금이 아니다. git은 저장소를 잠그지 않으므로,
다른 곳에서 같은 저장소를 클론해 쓰고 있는 것과는 아무 관계가 없다.

### 로컬 검증 환경

- 페이지는 three.js를 **unpkg CDN에서 직접 받는다**. 빌드 도구가 없다.
- 조직 egress 정책으로 unpkg가 막힌 환경이 있다. 그때는 `registry.npmjs.org`에서
  **같은 버전(0.160.1)** 을 받아 로컬에서만 대체한다. **레포에는 커밋하지 않는다.**
- GLB를 불러오므로 `file://`로는 안 되고 http 서버가 필요하다.

### 헤드리스에서 씬을 검증할 때

- `_animate`의 `dt`는 **0.05로 클램프**된다. 소프트웨어 렌더러처럼 느린 환경에서는
  벽시계 시간과 씬 시간이 크게 어긋난다. **`setTimeout`으로 기다리지 말고 씬 상태를 폴링할 것**
  (`_timeT`, `_camT`, `_shoreT`가 1에 도달했는지 등).
- `visible`은 활성 단계에서만 켜진다(`ACTIVE_STEPS`). `splash`에서 렌더 프레임이 0인 건 정상이다.
- `rAF`는 창이 숨겨지면 멈춘다. 그런 환경에서 전환을 확인하려면 카메라를 직접 세팅해 렌더하거나
  `requestAnimationFrame`을 타이머로 대체해야 한다.
- 색을 픽셀로 재려면 **배를 관통하지 않는 열**을 골라야 한다. 화면 중앙 세로선은 배가 가린다.

### 이미 측정해 둔 값들

10장에 GLB 노드별 바운딩박스와 측정값(파도 폭주, 배 점유율, 그림자 길이, 병 프로파일,
안개 대 타일 거리, 이징 곡선 비교)이 정리돼 있다. **다시 재기 전에 10장을 먼저 볼 것.**

---

## 12. 모델링 관련 대기 목록 (사용자 확인 대기 중)

**"내가 할 일 뭐 있었지?" 하면 이 장을 읽어 나열할 것.**
페이지 작업을 먼저 하기로 하고 잠시 미뤄둔 항목들이다. 사용자가 블렌더 쪽 결정을 내려야
진행할 수 있다.

### A. 새 `Scene.glb` 받기 (사용자 작업 중)

사용자가 블렌더에서 배 모델링을 고치고 새 소품들을 추가해 다시 뽑기로 했다.
`Scene.baked.glb`가 아니라 **원본 `Scene.glb`를 편집**하는 쪽으로 정했다.
(baked를 그대로 덮으면 섬이 두 번 밀리고 뱃머리가 두 번 돌아간다 — 11장 참조)

받으면 할 일:
1. `assets/Scene.glb` 교체
2. 노드 목록을 뽑아 **"배선됨 / 미배선"** 으로 정리해 사용자에게 보여주기
3. 하드코딩된 두 값을 다시 재서 반영 — `islandRestX`의 `0.479`(Beachhouse가 자기 원점
   기준 앞으로 뻗는 거리)와 `0.49`(선체가 배 원점 기준 뻗는 거리)
4. `SHIP_GLB_POS`(GLB의 Ship 노드 translation)도 바뀌었으면 옮겨 적기
5. `Scene.baked.glb` 다시 굽기

**절대 바뀌면 안 되는 노드 이름 일곱:**
`Rock` `Beachhouse` `Ship` `Seagull` `Tube` `Cabin` `Funnel`
(`Seagull`·`Tube`만 콘솔 assert가 있고, `Cabin`·`Funnel`은 경고 없이 조용히 안 먹는다)

### B. 자세별 소품 배치 — 설계까지 끝, 사용자 답변 대기

배가 섬을 향할 때와 등질 때 **소품이 붙는 자리를 다르게** 하고 싶다는 요구.
배는 두 자세를 오가는 게 아니라 1~2초에 걸쳐 실제로 180도 선회하므로, 인스턴스 두 개를
번갈아 보여주는 방식이 필요하다.

합의된 구조 — 접미사로 짝을 자동으로 찾는 방식:

```
Ship
├ Tube_Away    [Empty]   ← 섬을 등질 때 (stay / leaving / between)
│   └ 링크드 복제 튜브들
└ Tube_Toward  [Empty]   ← 섬을 향할 때 (returned / unsure)
    └ 링크드 복제 튜브들
```

코드가 `Ship` 아래에서 `_Away` / `_Toward` 접미사를 훑어 짝을 만들고 자세에 따라 하나만
켠다. 키워드 토글(`KEYWORD_PROP`)은 그 위에 얹힌다 — "키워드가 켜라 했고 자세도 맞을 때"만 보인다.
접미사 없는 노드는 지금처럼 자세와 무관하게 동작시켜 하위 호환을 유지한다.

**용량 걱정은 없다** (실측): glTF는 노드가 메쉬를 가리키기만 하므로 블렌더에서
링크드 복제(`Alt+D`)로 만들면 두 번째 노드는 약 112바이트다. 일반 복제(`Shift+D`)는
튜브 지오메트리 21,840바이트를 통째로 복사한다 — 195배 차이.
지금 GLB는 메쉬 공유가 0개라 그동안 일반 복제로 작업한 것으로 보인다.

**사용자에게 물어봐야 할 것 (아직 답 못 받음):**
1. "튜브를 두 개로" 가 자세별 버전 두 개인지, 구명튜브 자체를 두 개 다는 것인지
2. 선회 중 교체 시점 — 선체가 카메라에 가장 얇게 보일 때 교체(추천) / 크로스페이드 /
   선회가 끝난 뒤 교체
3. 접미사를 `_Away` / `_Toward` 로 갈지

### C. 하드코딩된 치수를 지오메트리에서 자동으로 재기 (제안했으나 답 못 받음)

`islandRestX`의 `0.479`와 `0.49`를 GLB 지오메트리에서 직접 재도록 바꾸면, 앞으로 집이나
배 크기를 바꿔도 코드에서 손댈 값이 없어진다. 사용자가 하겠다고 하면 진행.

### D. 이미 처리한 것 (참고)

- 갈매기 부위 삭제 로직 제거 — "4마리가 겹쳐 있다"는 오판이었고, 실제로는 한 마리의
  몸통/부리/발/날개끝이었다. 부리와 발이 지워지고 있었다.
