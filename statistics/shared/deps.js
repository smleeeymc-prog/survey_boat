/* =============================================================================
 * deps.js — 외부 의존성 버전. 두 화면이 이 파일 하나를 같이 본다.
 *
 * importmap은 HTML 안에 인라인이어야 하고 JS 값을 참조할 수 없다. 그래서 버전 문자열
 * 자체는 두 index.html에 각각 적힐 수밖에 없다 — 통합이 구조적으로 불가능한 유일한
 * 항목이다. 대신 "선언한 버전"을 여기 한 곳에 두고, 양쪽 selfCheck가 실제로 로드된
 * three의 리비전과 대조한다. 한쪽 importmap만 올리면 그 화면 콘솔에서 바로 걸린다.
 *
 * 올릴 때 고칠 곳 (셋 다 같이):
 *   1. 이 파일
 *   2. ../../index.html 의 importmap
 *   3. ../index.html 의 importmap
 * ========================================================================== */

export const THREE_VERSION = "0.160.1";

// three는 REVISION에 메이저만 담는다 ("0.160.1" → "160").
export const THREE_REVISION = THREE_VERSION.split(".")[1];
